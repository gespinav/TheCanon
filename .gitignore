# Local backups / pre-edit snapshots
*.bak.*
*.pre-*
index.html.backup

# Firebase CLI hosting cache (regenerated per deploy)
.firebase/

# Local Claude Code state (not source of truth)
.claude/

# Screenshots / images dropped into the project root for sharing
Screenshot*.png
Screenshot*.jpg
Screenshot*.jpeg

# Locally-installed tools
google-cloud-sdk/

# Drift-check report files generated by the scheduled agent
canon-*-drift-*.md

# Strategic plan / private planning docs — kept local only
canon-app-strategic-plan*.md

# Internal planning / reports / QA data — kept local only (repo is PUBLIC)
Canon-Roadmap-To-Market.docx
canon-1-1-4-report.md
canon-status-*.md
canon-phase3-setup-guide.md
.verified-ids.json
# Any Word doc (incl. .docx.bak backups) is internal planning — never publish
*.docx
*.docx.bak

# Python build artifacts
__pycache__/
*.pyc

# Node / Capacitor (native shell, §4)
node_modules/
# www/ is generated by `npm run build:web` from the root PWA files
www/
# Capacitor native build outputs (the android/ project itself IS committed)
android/app/build/
android/build/
android/.gradle/
android/app/src/main/assets/public/
android/app/src/main/assets/capacitor.config.json
android/app/src/main/assets/capacitor.plugins.json
.capacitor/

# Internal tooling — kept local only (repo is PUBLIC).
# build-roadmap-doc.py embeds the entire go-to-market plan inline (budgets,
# launch timeline, competitive analysis, risk register, ToS posture notes).
# The film-data scripts document third-party scraping methodology.
# Neither belongs in a public repo. tools/ci-validate.mjs stays (CI needs it).
tools/build-roadmap-doc.py
tools/apply-film-data.py
tools/fetch-film-data.py
tools/fixup-film-data.py
tools/final-gap-fix.py
